apiVersion: v1
kind: Service
metadata:
  name: notification-api-service
  namespace: video-processor
spec:
  $patch: replace
  selector:
    app: notification-api-service
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: http
      nodePort: 32515