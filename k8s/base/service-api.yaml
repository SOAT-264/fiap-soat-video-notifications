apiVersion: v1
kind: Service
metadata:
  name: notification-api-service
  namespace: video-processor
spec:
  selector:
    app: notification-api-service
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: http